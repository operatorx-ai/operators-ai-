// Playwright smoke tests moved to e2e/smoke.spec.ts
// This placeholder prevents the Playwright test definitions from being
// executed by Bun's test runner while preserving the original path.

export {};
